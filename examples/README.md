# Examples

Files in this directory are illustrative command/output captures intended to make the CLI understandable before installation. Exact numeric benchmark output can vary with command parameters and code revision; generated result JSON should record its seed and schema version.
