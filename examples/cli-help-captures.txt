Generated by CI on 2026-08-13.

$ cuc-habitat --help
usage: cuc-habitat [-h] {run,compare,evaluate} ...

Run CUC Habitat benchmark episodes.

positional arguments:
  {run,compare,evaluate}
    run                 run one Habitat episode
    compare             run all implemented agents with the same conditions
    evaluate            run multi-seed evaluation reports

options:
  -h, --help            show this help message and exit

$ cuc-habitat run --help
usage: cuc-habitat run [-h] [--agent {alpha,beta,delta,epsilon,gamma}]
                       [--turns TURNS] [--seed SEED]
                       [--episode {energy-shortage,hidden-maintenance-opportunity,misleading-urgency,overload-competing-signals,quiet-world-drift,storage-disorder}]
                       [--observation-only]
                       [--observation-noise OBSERVATION_NOISE] [--no-render]
                       [--json-out JSON_OUT]

options:
  -h, --help            show this help message and exit
  --agent {alpha,beta,delta,epsilon,gamma}
  --turns TURNS
  --seed SEED
  --episode {energy-shortage,hidden-maintenance-opportunity,misleading-urgency,overload-competing-signals,quiet-world-drift,storage-disorder}
  --observation-only    act through a detached BrailleStream observation
  --observation-noise OBSERVATION_NOISE
                        perception degradation from 0.0 to 1.0
  --no-render           suppress turn-by-turn display
  --json-out JSON_OUT   optional path to save episode result JSON
